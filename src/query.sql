-- „Koncerty" — wpisz zapytania między znaczniki BEGIN/END danego podzadania.
-- Dostępne tabele: zespoly(id_zespolu, nazwa, liczba_artystow),
--                  miasta(kod_miasta, miasto, wojewodztwo),
--                  koncerty(id, id_zespolu, kod_miasta, data).
-- Nie usuwaj linii ze znacznikami `-- task_N BEGIN` / `-- task_N END`.

-- Miejsce na zapytania pomocnicze — NIE jest oceniane.
-- playground BEGIN

-- playground END


-- task_1 BEGIN
-- Podzadanie 1: ile koncertów odbyło się w lipcu?
-- task_1 END


-- task_2 BEGIN
-- Podzadanie 2: nazwa miasta, w którym wystąpiło łącznie najwięcej artystów
-- (zespół występujący w danym mieście kilka razy liczony raz); jeśli miast jest kilka — wszystkie.
-- task_2 END


-- task_3 BEGIN
-- Podzadanie 3: dla każdego województwa średnia liczba koncertów na jedno miasto województwa,
-- zaokrąglona do dwóch miejsc po przecinku, posortowana malejąco.
-- task_3 END


-- task_4 BEGIN
-- Podzadanie 4: nazwy zespołów, które nie koncertowały w okresie 20–25 lipca 2017 włącznie.
-- task_4 END


-- task_5 BEGIN
-- Podzadanie 5: nazwy zespołów grających częściej w weekendy niż w dni powszednie;
-- dla każdego liczba koncertów w weekendy oraz liczba koncertów w dni powszednie.
-- task_5 END
